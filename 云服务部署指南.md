# AI 插件云端部署与接入指南

本文档旨在指导如何将带有 AI 插件的 `soulmirror_app.py` 项目部署到云端服务器，以便分享给其他用户访问和使用。

---

## 场景一：本地运行 或 使用自有云服务器 (VPS)
如果你是在自己的电脑上运行，或者购买了一台独立的云服务器（如阿里云、腾讯云、AWS 等），你**不需要做任何额外配置**。

**运行方式**：
直接在项目根目录下启动 Streamlit 即可：
```bash
streamlit run soulmirror_app.py
```
*原理解析*：代码中已经内置了自动探针。当启动主程序时，系统会自动检测 `8787` 端口。如果未被占用，它会在后台自动拉起 `packages/plugin-server` 的 Node.js 后端服务。你无需再开多个终端去单独执行 `npm run dev`，一切已完全自动化。

---

## 场景二：部署到公有云（如 Streamlit Cloud）分享给他人
如果你希望把前端部署在 **Streamlit Community Cloud** 等免费云平台上供网友访问，由于 Streamlit 云环境通常不包含完整的 Node.js 运行环境，且对外只开放 8501 的网页端口，你需要将 **AI 插件后端** 单独部署到一个 Node.js 托管平台（如 Vercel、Zeabur 等）。

### 步骤 1：部署后端服务 (以 Vercel 为例，永久免费)
本项目已经为你配置好了 Vercel 的部署文件 (`packages/plugin-server/vercel.json`)，可以直接一键部署。

1. 将你的完整代码（包括 `packages` 文件夹）推送到你的 GitHub 仓库。
2. 注册并登录 [Vercel](https://vercel.com/)。
3. 点击 **Add New... -> Project**，授权并导入你的 GitHub 仓库。
4. 在 Vercel 的项目配置（Configure Project）页面中：
   - 找到 **Root Directory** (根目录) 选项。
   - 点击 Edit，选择 `packages/plugin-server` 目录并保存。
   - （可选）展开 **Environment Variables**，如果你的后端需要 API 密钥（如 Supabase 或火山引擎的 Key），请在这里添加。
5. 点击 **Deploy** 进行部署。
6. 部署成功后，Vercel 会分配给你一个公网域名（例如：`https://my-ai-plugin-backend.vercel.app`）。请复制并保存这个链接。

### 步骤 2：修改 Streamlit 代码中的后端地址
打开项目根目录下的 `soulmirror_app.py`，找到代码中负责加载 Web Component 的两处 `<script>` 标签代码段（分别在 `show_resonance_demo` 和 `show_ai_plugin` 函数内部）。

找到以下代码：
```javascript
// ==========================================
// 后端部署配置提示：
// 如果你把应用部署在 Streamlit Cloud 等公有云，
// 请将这里的 base 改为你单独部署的后端公网地址 (例如 Render/Vercel URL)
// ==========================================
const host = window.location.hostname || "localhost";
const base = `http://${host}:8787`;
```

**将 `base` 变量的赋值替换为你刚才在 Vercel 获得的域名：**
```javascript
// 注意：域名结尾不要加斜杠 /
const base = "https://my-ai-plugin-backend.vercel.app";
```

### 步骤 3：部署前端到 Streamlit Cloud
1. 将修改后的 `soulmirror_app.py` 提交并推送到 GitHub 仓库。
2. 登录 [Streamlit Community Cloud](https://share.streamlit.io/)，点击 **New app**。
3. 选择你的仓库，主文件路径填入 `soulmirror_app.py`。
4. 在 Advanced Settings 中填入你前端所需的 Secrets（如 `SUPABASE_URL`、`SUPABASE_KEY` 等）。
5. 点击 **Deploy**。

部署完成后，任何人都可以通过你的 Streamlit 专属链接访问网站，所有的 AI 插件聊天和文生图功能都将通过 Vercel 上的云端接口进行交互，不再依赖本地的 `npm run dev`！