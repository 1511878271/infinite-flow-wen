# 网站更新与部署操作指南

这份文档记录了当你在本地（Windows 电脑）修改了代码后，如何将最新代码更新到腾讯云服务器上。

---

## 第一步：在本地完成修改并打包

1. **保存并测试**：确保你在本地编辑器中修改的代码已经全部保存，并且在本地运行测试没有问题。
2. **清理无用文件（强烈推荐）**：打包前，请确保**不要打包**以下文件或文件夹，否则可能会导致上传慢或覆盖掉服务器配置：
   - ❌ `node_modules` 文件夹（体积太大，影响上传速度）。
   - ❌ `.env` 等环境变量文件（**千万别打包**，因为本地的 `.env` 是你测试用的，一旦覆盖掉服务器的 `.env`，会导致服务器端口错乱或安全 Token 丢失！）。
3. **打包为 ZIP 压缩包**：
   - 进入你的项目根目录 `chajian` 文件夹内部，全选需要打包的代码文件（避开上面提到的 `node_modules` 和 `.env`）。
   - 右键点击选中的文件，选择 **7-Zip** -> **添加到压缩包...**
   - **非常重要**：在“压缩格式”中，**必须选择 `zip`**（千万不要选 7z 或 rar）。
   - 将生成的 `chajian.zip` 文件放到 `D盘` 根目录（即 `D:\chajian.zip`），以便后续命令保持一致。

---

## 第二步：将代码上传到服务器

1. 打开 Windows 电脑自带的 **PowerShell**。
2. 复制并运行以下命令，将代码上传到服务器的 `/srv/chajian/` 目录下：

```powershell
scp -i "$env:USERPROFILE\Downloads\Hitech.pem" "D:\chajian.zip" ubuntu@124.221.41.115:/srv/chajian/
```

*(注意：如果你的 `.pem` 密钥文件或压缩包路径有变，请自行替换命令中的路径。)*

**如果 `.pem` 密钥文件的位置发生改变，你该怎么操作？**

如果某天你不小心移动了 `Hitech.pem`（例如从 `Downloads` 文件夹移到了 `C:\Users\mao\Desktop\Hitech.pem`），你需要：
1. **替换上传命令中的路径**：把命令中 `-i` 后面的路径换成你的新位置。
   比如新路径是桌面的话，命令就变成了：
   ```powershell
   scp -i "C:\Users\mao\Desktop\Hitech.pem" "D:\chajian.zip" ubuntu@124.221.41.115:/srv/chajian/
   ```
2. **重新修复权限（极其重要）**：Windows 上的 `ssh` 或 `scp` 命令对私钥文件极其严格。如果你移动或复制了 `.pem` 文件，它可能会继承新文件夹的开放权限，导致报错 `UNPROTECTED PRIVATE KEY FILE`。此时，你需要**重新执行权限限制命令**（以移到桌面为例）：
   ```powershell
   # 1. 移除继承权限
   icacls "C:\Users\mao\Desktop\Hitech.pem" /inheritance:r
   # 2. 授予当前用户完全控制权
   icacls "C:\Users\mao\Desktop\Hitech.pem" /grant:r "$($env:USERNAME):(F)"
   ```
   **建议**：为了避免麻烦，强烈建议把 `.pem` 文件固定放在某个地方（比如 `Downloads` 文件夹里），不要随意移动。

---

## 第三步：登录服务器并解压覆盖

1. 打开腾讯云控制台，使用 **WebSSH** 登录到你的服务器。
2. 依次输入以下命令进入目录并解压：

```bash
# 1. 进入服务器上存放项目的目录
cd /srv/chajian

# 2. 解压刚上传的压缩包（-o 参数表示直接覆盖旧文件，不会弹出确认提示）
unzip -o chajian.zip
```

---

## 第四步：重新编译并重启服务

代码覆盖后，需要重新编译前端页面和后端服务，并让后台程序重启。

```bash
# 1. 进入项目根目录
cd /srv/chajian/chajian

# 2. （如果添加了新插件或依赖）安装依赖包
npm install

# 3. 重新编译所有项目（这会自动编译 web-app, plugin-ui 和 plugin-server）
npm run build

# 4. 重启后端的 Node.js 服务（如果之前已经用 pm2 跑起来了）
pm2 restart plugin-server
```

---

## 第五步：运行程序与验证

### A. 第一次在服务器启动（还没有 pm2 进程时）

```bash
cd /srv/chajian/chajian
pm2 start npm --name plugin-server -- start
pm2 save
```

### B. 日常更新后的启动/重启

```bash
pm2 restart plugin-server
```

### C. 验证是否运行成功

```bash
# 1) 查看进程是否在线
pm2 status

# 2) 查看实时日志（按 Ctrl+C 退出）
pm2 logs plugin-server

# 3) 测试后端健康检查（后端直连端口）
curl -i http://127.0.0.1:8787/health

# 4) 测试通过 Nginx 的健康检查（如果你配置了 /health 反代）
curl -i http://127.0.0.1/health
```

如果 `curl` 返回类似 `{"ok":true}`，表示服务正常。

---

## 常见问题排查

- **前端页面没变化？**
  可能是浏览器缓存。请在浏览器中按 `Ctrl + F5` 强制刷新，或者清理浏览器缓存后再试。
  
- **上传时提示 Permission denied 或超时？**
  检查你的电脑是否连接了会拦截 22 端口的校园网/公司网，尝试切换手机热点。检查 `.pem` 文件是否仍在 Downloads 文件夹中。

- **解压时报错 End-of-central-directory signature not found？**
  说明你上传的压缩包损坏，或者你打包成了非标准的 zip 格式（比如 .7z）。请在本地重新使用 7-Zip 打包为标准的 `.zip` 格式再上传。
