# AI 插件嵌入操作指南

这份文档介绍了如何将你的“文生图 & 图生3D”AI 插件，作为一个独立组件（Web Component）嵌入到**任何其他网站**（比如你的个人博客、公司主页、甚至朋友开发的网页）中。

---

## 1. 准备你的服务端（插件中心）

为了让别人能调用你的插件，你必须首先在你的服务器或电脑上**启动并公开**你的插件后端服务。

### 1.1 配置访问密钥 (Token)
防止别人恶意调用你的 AI 接口消耗额度，请在后端的 `.env` 文件中设置密钥：
1. 打开文件：`packages/plugin-server/.env`
2. 确保有这一行配置（可以自定义一串复杂的密码）：
   ```env
   PLUGIN_REQUIRED_TOKEN=my_secret_token_12345
   ```

### 1.2 打包并启动生产服务
在你的终端（项目根目录 `C:\Users\mao\Desktop\chajian`）依次执行以下命令：

1. **打包最新代码**（生成给别人用的前端组件文件）：
   ```bash
   npm run build
   ```
2. **启动服务**：
   ```bash
   npm run start
   ```
   > 启动成功后，你的服务器会运行在 `8787` 端口。如果是在云服务器上，假设你的域名或 IP 是 `http://your-domain.com`。

---

## 2. 发给接入方的操作步骤

你可以直接把以下这段话复制发给需要接入你插件的人：

> **“你好，请在你的网站需要展示 AI 面板的地方，加入以下两段 HTML 代码：”**

### 步骤 A：引入组件脚本
在网页的 `<head>` 或 `<body>` 末尾引入脚本（确保将 `http://your-domain.com` 替换为你的真实服务器地址）：

```html
<!-- 引入 AI 插件组件库 -->
<script type="module" src="http://your-domain.com/embed/ai-plugin-wc.js"></script>
```

*(注意：如果你在本地测试，就把 `your-domain.com` 换成 `localhost:8787`)*

### 步骤 B：放置插件面板标签
在网页中任何你想要显示该插件的地方，放入这个自定义标签：

```html
<!-- 渲染 AI 插件面板 -->
<ai-plugin-panel 
  api-base-url="http://your-domain.com" 
  token="my_secret_token_12345"
  theme="dark">
</ai-plugin-panel>
```

#### 标签参数说明：
*   `api-base-url`: **必须**。填写你的服务器访问地址。
*   `token`: **必须**。填写你分配给他的密钥，必须与你 `.env` 中的 `PLUGIN_REQUIRED_TOKEN` 保持一致。
*   `theme`: **可选**。面板主题，支持 `dark`（深色） 或 `light`（浅色）。默认深色。

---

## 3. 本地快速测试验证

如果你想立刻验证这个嵌入效果：
1. 确保你已经跑过 `npm run build` 和 `npm run start`。
2. 在桌面上新建一个空的 `test.html` 文件。
3. 把以下内容粘贴进去并保存：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>插件嵌入测试</title>
  <style>
    body { background-color: #f0f0f0; display: flex; justify-content: center; padding-top: 50px; }
  </style>
</head>
<body>

  <!-- 这里是你嵌入的组件 -->
  <ai-plugin-panel 
    api-base-url="http://localhost:8787" 
    token="这里填你的Token"
    theme="dark">
  </ai-plugin-panel>

  <!-- 引入脚本 -->
  <script type="module" src="http://localhost:8787/embed/ai-plugin-wc.js"></script>
</body>
</html>
```

4. 双击在浏览器中打开 `test.html`，你就能看到面板成功运行在一个白底的普通网页中了！